import 'package:flutter/material.dart';

class ClocksTab extends StatelessWidget {
  const ClocksTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("clocks")),
    );
  }
}
