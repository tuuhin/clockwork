import 'package:flutter/material.dart';

class TimmerTab extends StatelessWidget {
  const TimmerTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("timmer")),
    );
  }
}
